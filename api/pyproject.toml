[project]
name = "startup-readiness-mvp-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "google-genai",
  "psycopg2-binary",
  "pydantic",
  "PyMuPDF",
  "python-dotenv",
  "python-multipart",
  "supabase",
  "uvicorn",
]

[tool.vercel]
entrypoint = "api_endpoints:app"
